#[cfg(test)]
mod test {
    use corkscrew::builder::build;
    use reqwest::Client;
    use std::error::Error;

    async fn test_build_requests() -> Result<(), Box<dyn Error>> {
        Ok(())
    }
}
